# 识文新裁 — 简历项目段更新稿（重构计划落地版）

> 版本：v1.1　|　日期：2026-08-24　|　作者：马欣仪
> 关联：`识文新裁-完整交付文档.md`（§2 功能复原 + §4 评测指标 + §11 面试口径）
> 用法：检索线数字已从 `eval/report.md` 实跑回填（100 题，2026-08-24）；辩论/写作/角色扮演线仍为「S_x 待填」，出数后再替换。
>
> ⚠️ **数字口径（重要）**：旧版「任务成功率 74%→89%」「长对话一致性 81%→93%」两个百分比**已移除**——无真实评测背书，面试深挖会崩（Shopee 一面已踩雷，定调"诚实降级"）。检索线数字**已由 `eval/run.py` 在 100 题评测集上实跑得出**，可复现、可被追问细节；辩论/写作/角色扮演线尚未出数，面试口径见交付文档 §11。

---

## 一、重构后目标版（检索线已实跑出数，其余待 S7 出数后回填）

**项目技术**：Python · LangGraph · FastAPI · PostgreSQL · Milvus · Redis · Docker · BGE-M3 · DeepSeek API · MCP

**项目介绍**：贯穿古籍"数字化整理 → 学术化研究 → 大众化传播"全生命周期的 AI 平台，以 LangGraph 工作流图编排研微（学术研究助手）/ 新裁（沉浸式交互文创）/ 识文（智能整理）三大模块；检索图一处构建、问答 / 辩论 / 写作三处复用。

**个人职责与成果：**

1. **古籍垂类 RAG 检索图（图工程 + 多跳反思）**：以 LangGraph 工作流图显式建模"离线建库 → 在线检索 → 生成 → 校验"全流程；结构感知切分（卷/篇/章）+ 元数据抽取（书/版本/作者/朝代）后，BGE-M3 向量化入 Milvus、元数据入 PostgreSQL；在线三路混合检索（元数据前置过滤 + 向量 + BM25 字符二元组）经 RRF 融合，输出带引据回答并做 grounding 校验，失败先经反思节点诊断"why miss"（实体抽取错 / Query 过宽 / 语料缺失）再改写重检索，≤3 轮收敛。以**引据合规率**（引用书·篇·版本与检索源一致率）为头号指标。100 题评测（考据50/翻译20/知识30，覆盖20部古籍，每题标 golden 书·篇）实跑：**引据合规率 71/73=97.3%**（分母=带篇目标注的题：考据 48/50、翻译 20/20；开放性知识题无单一篇目出处、不计入）、**Recall@5 91%、MRR 0.72、Faithfulness 4.63/5**，平均 1.55 轮收敛；多跳闭环实测生效（如"庄周梦蝶"首轮检索漏检 → 反思改写"齐物论" → 重检索命中 → 正确引据，2 跳闭环可复现）。

2. **Multi-Agent 先贤辩论与人设维稳**：紧急度评分（话题相关度、上轮发言者、用户情绪/追问强度）驱动孔子/孟子/老子/韩非子等多先贤智能体发言权仲裁与自主插话/沉默；每位先贤为"人设 prompt + RAG 子图"subgraph，论点可回溯原著；滑动窗口余弦相似度**人格漂移软监测** + RAG 长时记忆修正偏差（监测纠偏、不硬性限制表达）。［S7 待填：论据引用率 / BaiJia 六维 LLM-judge］

3. **Agent 状态与上下文治理**：分层记忆（短期对话 / 长期人设 / 任务状态）承接"状态持久化与断点恢复"，token 预算跟踪 + 检索上下文压缩 + 动态裁剪上下文窗口控制长对话成本；轻量人物关系表（PostgreSQL 手工维护，人物→身份/时代/著作/师承）使关系类问题 SQL join 直答。［S7 待填：长对话 token 节省比 / 多轮收敛率］

4. **研究写作图 + MCP 工具层**：写作图（选题→大纲→逐节检索→逐节写作→综合润色）自动产出带引据的结构化研究文章；检索 / 引据校验 / 人物关系查询 / 写作引擎统一封装为 **MCP 工具**，多图复用。［S7 待填：3 选题写作引据准确率］

> 若嫌 4 条太长：可把 3、4 合并为一条「Agent 状态治理 + 工具工程」，或砍掉 4 里的写作图只留 MCP。

---

## 二、当前投递期过渡写法（检索线已有数字，其余待出）

检索线可直接用 §一 第 1 条的实跑数字（97.3% / 91% / 0.72 / 4.63）；辩论、写作、角色扮演线**暂不写百分比**。如遇面试官追问"之前简历不是有 74%→89% 吗"，用交付文档 §11 的"诚实降级三步"：
1. 直接承认旧百分比无真实评测支撑，是项目早期自评；
2. 说明已重构为图工程后端，检索线已跑出可复现真数字（`eval/run.py`，100 题），辩论/角色扮演评测在途；
3. 给出可讲的实证：检索链路全通、golden 引据基线通过、100 题评测集已建、多跳反思闭环有实跑用例。

---

## 三、面试讲法要点（每个 bullet 一句话，防被问倒）

| 简历句 | 必被追问 | 一句话答法 |
|---|---|---|
| 三路混合检索 | 为什么三路，不一路 | 元数据过滤挡"版本合规"（古籍张冠李戴=事故）、向量挡语义、BM25 挡古词精确命中；BM25 用 jieba 词+字符二元组双 token（成语"庄周梦蝶"与原文"胡蝶"词形失配时靠 bigram 兜底）；RRF 无权重融合，先跑通再上精排 |
| 反思节点 | 怎么判"该反思" | grounding 校验不通过且轮数<N，先诊断 why miss（实体错/Query 宽/语料缺）再决定改写策略，不是盲改。落地细节：回答"无法确定"但检索里已有相关书篇 → 判不合格强制反思（实测"庄周梦蝶"首轮漏检 → 改写"齐物论" → 2 跳命中） |
| 多跳收敛 | 终止条件 | grounding 通过即停；轮数耗尽给出"已尽力 + 缺口提示" |
| 紧急度评分 | 公式 | 加权和：话题相关度 ×w1 + 距上轮发言时长 ×w2 + 用户情绪/追问强度 ×w3，top1 发言 |
| 软监测 vs 硬约束 | 为什么不硬约束 | 事实禁止会压缩先贤表达空间；用余弦相似度监测漂移 + 记忆修正，监测纠偏不禁止（用户定调） |
| 分层记忆 | CAVM 去哪了 | 诚实：旧版 CAVM 沙箱变量架构是早期设计，重构用 LangGraph checkpoint + 分层记忆落地同一目标（状态持久化 / 断点恢复 / 上下文治理），代码里是真实现 |
| MCP 工具层 | 和普通 function calling 区别 | 标准化协议、跨进程跨语言、工具可被外部客户端直接复用；Pico 侧也做了 MCP → 两个项目组合叙事 |
| 人物关系表 | 为什么不上 GraphRAG | 评估过完整图谱：抽取质量不稳易幻觉、简历未承诺、与"软约束"定位冲突；MVP 用元数据+关系表覆盖主要关系题，避免不可控抽取链路——这个取舍本身就是工程判断 |
| 引据合规 | 怎么保证版本不错 | 元数据前置过滤（PG 硬筛版本）+ 检索 chunk 元数据与引用强制一致校验，不合格进反思重检 |
| Milvus 为什么 | 为什么不用 PGvector | 简历已写 Milvus + 面试前会扩：Milvus 是专用向量库，标量过滤+向量检索混合查询、索引类型、生产独立部署 |

---

## 四、数字来源（S7 后回填，别自己编）

| 指标 | 来源 | 评测集 |
|---|---|---|
| Recall@5 91% / MRR 0.72 / 引据合规率 71/73=97.3%（分母=带篇目标注题） / Faithfulness 4.63 | 检索评测（已实跑，`eval/run.py`） | 研微问答 100 题（考据50+翻译20+知识30，覆盖20部古籍） |
| 论据引用率 / 事实一致性 | 辩论评测 | 辩论 5 辩题 |
| BaiJia 六维（CC/DA/CA/EI/CR/CHA） | LLM-judge | 角色扮演 20 问 |
| CHRF++ / BERTScore | 翻译评测 | 翻译 50 句 |
| CER | OCR（可选） | AncientDoc OCR 任务 |
| 端到端延迟 P50/P95、token 成本 | 系统观测 | 线上 trace |

评测集规格与构建方法见 `识文新裁-完整交付文档.md` §4。
