# 本地开发用：本地 BGE-M3 embedding（服务器走 Cloudflare API，不需要这些）
# 仅当 EMBEDDING_PROVIDER=local_bge_m3 时才需要，避免拖垮 2G 内存的小服务器。
#
# 安装（torch 用 CPU 版，避免拉 CUDA 全家桶）：
#   pip install "torch>=2.0" --index-url https://download.pytorch.org/whl/cpu
#   pip install -r requirements-local.txt

sentence-transformers>=2.7
